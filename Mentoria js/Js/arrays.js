
// const array = [1, 2, 3 ];
const nombres = ["aduardo" , "joan", "carlos"];
const nombresDos = [""];
const numeros = new Array("2" , 3, 5,6 );

let arregloNuevo;

// forma de escojer un elemento del array

// console.log(numeros);

// metodos de Arrays ----------------------------

// // concat() - Une dos o más arrays

    arregloNuevo = numeros.concat(nombres);

    console.log(arregloNuevo);

// indexOf() - Busca un elemento en el array y devuelve su posición. Busca desde el principio y a partir de la primera posición si no se especifica nada

    // arregloNuevo = numeros.indexOf(2);
    
    // console.log(arregloNuevo);

// join() - Junta los elementos de un array en una cadena con un separador – opcional.

    // let resultado = nombres.join(" ");
    // console.log(resultado);

    // console.log(nombres);

// pop() -  Borra el último elemento del array y devuelve su contenido. 

    // let resultado = nombres.pop();
    // console.log(resultado);

// push - Añade nuevos elementos al array y devuelve su nueva longitud.

    // let arrayDos = [1,2,4];
    // nombres.push(arrayDos);
    // console.log(nombres);      

// reverse() - Invierte el orden de los elementos del array.

    // nombres.reverse();
    // console.log(nombres);      


// splice() -  Añade o elimina elementos de un array.

    // nombres.splice( 1, 0, "maria");
    // console.log(nombres);     


// split() -  Convierte un string en un array.

    // let frase = "hola mundo como estas hola";

    // let resultado = frase.split(" " , frase.length);
    // console.log(resultado);       

// sort - ordenar ascendentemente

//    let resultado = nombres.sort();
//    console.log(resultado);

